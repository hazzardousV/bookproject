<h1>All Books</h1>

<ul id="books">
	<% @books.each do |book| %>
		<li>
			<%= link_to book.title, book_path(book),  id: "book_#{book.id}" %>
			<%= book.author %>
		</li>
	<% end %>
</ul>

<%= link_to "Add a New Book", new_book_path, class:'new_book' %>