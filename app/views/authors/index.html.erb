<h1> All People </h1>
<ul id="people">
	<% @authors.each do |author| %>
		<li>
			<%= link_to author.name, author_path(author), id: "author_#{author.id}" %>
			<%= author.title %>
		</li>
	<% end %>
</ul>